name: Call Issue Spec Automation
on:
  repository_dispatch:
    types:
      - project_status_change
jobs:
  call-automation:
    uses: mehdimohammadise/.github/workflows/codex-github-issue-spec-kit-automation.yml
    secrets: inherit